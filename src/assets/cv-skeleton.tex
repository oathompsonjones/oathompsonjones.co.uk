% Define the type of document.
\documentclass[12pt, a4paper]{article}

% Set a more sensible document size.
\usepackage[margin=1cm]{geometry}

% Set the fonts.
\usepackage[default]{lato}

% Prevent words from being split accross lines.
\tolerance=1\emergencystretch=\maxdimen\hyphenpenalty=10000\hbadness=10000

% Prevent error from biber.
\usepackage{biblatex}

% Set up hyperlinks.
\usepackage[hidelinks]{hyperref}

% Allows the use of multiple columns.
\usepackage{vwcol}

% Removes numbers from sections and the contents page.
\setcounter{secnumdepth}{0}

% Remove page numbers.
\pagenumbering{gobble}

% Styles the title.
\usepackage{xcolor}
\usepackage{titling}
    \pretitle{\centering\vskip-2.25cm\huge}
    \posttitle{\par}
    \preauthor{\centering}
    \postauthor{\par}
    \predate{}
    \postdate{}
    \renewcommand\maketitlehooka{\par\noindent{\color{lightgray}\titlerule[1pt]}\par}
    \renewcommand\maketitlehookb{\par\noindent{\color{lightgray}\titlerule[1pt]}\par}

% Style section sections.
\usepackage[compact]{titlesec}
\titleformat
    {\section} % command
    [block] % shape
    {\color{black}\normalfont\large\bfseries} % format
    {\thesection} % label
    {} % sep
    {\vskip0.1cm} % before-code
    [{\color{lightgray}\titlerule[2pt]}] % after-code
\titleformat
    {\subsection} % command
    [block] % shape
    {\color{black}\normalfont} % format
    {\thesubsection} % label
    {} % sep
    {\vskip0.05cm} % before-code
    [{\color{lightgray}\titlerule[1pt]}] % after-code

% Sets the title.
\title{Oliver Jones}
\author{
    \href{mailto:oathompsonones@gmail.com}{oathompsonones@gmail.com}
    $\bullet$
    07872 644095
    $\bullet$
    \href{https://oathompsonjones.co.uk}{oathompsonjones.co.uk}
}
\date{}

% Begin the document.
\begin{document}
\maketitle
\color{darkgray}

%CONTENT%

\end{document}
