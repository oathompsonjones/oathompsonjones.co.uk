import type { Metadata } from "next";
import { layout } from "components/layout";

export const metadata: Metadata = { title: "Oliver Jones | Articles - Coming Soon" };

export default layout({ className: "wrapper" });
