/*
The not-found.tsx file is not rendered when clicking links which go nowhere, unless the user also then refreshes their page.
This catches that issue.
*/

import Error from "app/not-found";
export default Error;
